// Quyidagi enumlarni hosil qiling, hamda dasturda ulardan foydalaning:
// CRUD, Action, UserRoles

enum CRUD {
  post,
  get,
  put,
  delete,
}

enum Action {
  playing,
  stopped,
  next,
  pre,
}

enum UserRole {
  admin,
  staff,
  superuser,
  user,
}
